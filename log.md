# Wiki log

Chronological, append-only log of what happened and when. Newest entry last.
Entry format:

`## [YYYY-MM-DD] <onboard|ingest|lint|query> | <subject>`

## [2026-06-19] onboard | dam-agents/dam @662ebe4
## [2026-06-19] ingest | dam-agents/dam @662ebe4 — eager first pass: 8 source pages, 7 concept pages, 7 entity pages; watermark set to 662ebe4.
## [2026-06-20] ingest | dam-agents/dam @4a48ae2 — delta from 662ebe4 (1 commit: unify provider definitions). Refreshed connections-and-contributions (new provider-preset registry note), re-pinned api-server/cli/ui source pages to 4a48ae2; watermark bumped.
## [2026-06-20] lint | re-pinned dam.md overview to @4a48ae2 (packages/ glob covered the provider-definitions refactor; module map unchanged). No contradictions, orphans, or broken links. All other pages left at their pinned commits.
## [2026-06-21] ingest | dam-agents/dam @0158357 — delta from 4a48ae2 (1 commit: unbreak from-scratch cluster install / keycloak theme type-check). Re-pinned dam.md overview to @0158357 (package.json keycloakify packageExtension); enriched entities/keycloak.md Theme section with keycloakify build (vite build && keycloakify build) and the pnpm peer-dep extension. Watermark bumped.
## [2026-06-21] lint | clean sweep at @0158357 — verified staleness across all 22 pages (no drift beyond ingest's dam.md/keycloak.md refresh), 0 orphans, 0 broken links, no contradictions. No coverage gaps surfaced.
## [2026-06-22] ingest | dam-agents/dam @9d1bc99 — delta from 0158357 (1 commit: feat(helm) custom annotations on chart workloads, #1378). Change is confined to deploy/helm/platform/ (new commonAnnotations/commonPodAnnotations + per-component annotations/podAnnotations knobs); no page's files: cover those paths and no load-bearing claim is affected, so no page edits. Watermark bumped. Coverage note for lint: the Helm chart has only module-map-level coverage in dam.md — no dedicated deployment page.
## [2026-06-22] lint | clean sweep at @9d1bc99 — verified staleness across all 22 pages (the only post-0158357 change, deploy/helm annotations #1378, intersects no page's files:), 0 orphans, 0 broken links, no contradictions. Coverage gap reaffirmed: the Helm chart (deploy/helm/platform/) has only module-map-level coverage in dam.md — a dedicated deployment page remains a candidate for a future pass.

## [2026-06-22] query | E2E testing in DAM
- Miss: wiki only mentioned `e2e` in one line of sources/dam.md. Read packages/e2e from dam-agents/dam @9d1bc99.
- Added pages/concepts/e2e-testing.md (Playwright serial-pipeline specs, mock agent + gated e2e tRPC control API, mise run e2e/e2e:loop/e2e:reset, injection + api-key specs).
- index.md: added the concept line. No contradictions found.
## [2026-06-23] ingest | dam-agents/dam @380cb06 — delta from 9d1bc99 (19 commits). Updated: connections-and-contributions.md (GitHub identity→gitconfig commit-attribution via best-effort `file` contribution at $HOME/.gitconfig #934; fixed stale Contribution union egress-host→egress-allow/egress-inject); cli.md (new `channel` group for Slack/Telegram bindings + allow-list #1385, connection `--config key=value` inputs #1497, plainer server-rejection/auth error reporting #1372/#1498); db.md + persistence-substrates.md (Postgres role separation ADR-071 #366: NOSUPERUSER app roles platform_apiserver/platform_keycloak per-db, revoked PUBLIC CONNECT, statement-logged DBA superuser, post-upgrade migrate-roles Job); e2e-testing.md (spec 07 now asserts foreign-fork credential injection); keycloak.md (keycloak-theme tsconfig React-v18 type pin #1446). Reviewed no-op: agent-runtime ACP idle-reap 3s debounce #1636 (below page altitude, no claim stale), agents model-gateway/pi-agent model discovery #1500/#394, ui files-panel menu/MCP-url/github-icon #1356/#1076/#1445 (sub-page granularity). Coverage gap reaffirmed for lint: Helm deploy fixes #1681/#1589/#1590 (pull policy, zero-downtime UI rollout, liveness probe tuning) and keycloak OTel chart passthrough #1443 have no dedicated deployment page. Watermark bumped to 380cb06.
## [2026-06-23] lint | at @380cb06 — re-pinned 15 pages drifted by the 9d1bc99..380cb06 delta whose files: moved but no load-bearing claim changed (verified): agent-client-protocol, agent-runtime (ACP reap-debounce/readline #1636), agents (model-gateway/pi-agent #1500/#394), ui (#1356/#1076/#1445), api-server (connections/agents modules; no new module), controller (liveness-probe TimeoutSeconds/FailureThreshold tuning #1590), dam (packages/ + pnpm-workspace version bumps only; module map unchanged), and the pages sharing the two additive architecture docs — persistence.md gained a three-role Postgres sentence (agent, schedule, template) and security-and-credentials.md gained a "Platform database roles" section (hitl-approvals, zero-trust, envoy-gateway, fork, keycloak). Added a zero-trust→db See-also cross-link for the new DB-role boundary (distinct from the egress gateway). 0 orphans, 0 broken links, no contradictions. agent-lifecycle/platform-topology/session left untouched (their source files did not move). Coverage gap reaffirmed: the Helm chart (deploy/helm/platform/) still has only module-map-level coverage in dam.md — the postgres role-separation config, keycloak OTel passthrough (#1443), and the pull-policy/zero-downtime-rollout/liveness fixes (#1681/#1589/#1590) remain candidates for a dedicated deployment page.
## [2026-06-23] query | skill resolution folders (#1637)
Miss → wrote concepts/skill-resolution.md pinned @cd63253. DAM scans `<repo>/skills/*` then repo root (first hit wins); dot-dirs skipped, so `.claude/skills/` is not discovered. Confirms issue #1637.
## [2026-06-24] ingest | dam-agents/dam @d34c21a — delta from 380cb06 (10 commits). Updated: skill-resolution.md (major rewrite — #1745/@8fa9761 now recognizes `.claude/skills/` and `.agents/skills/` alongside `skills/` via the shared `SKILL_SOURCE_ROOTS` constant; scan unions all three roots with top-level `*` as fallback-only, dedupes by skill name first-root-wins, install resolver walks the same order, api-server public-archive scan shares the constant; resolves the #1637 gap the page previously documented as a limitation); agents.md + dam.md (new **nous** harness #1382/@ca1877a — hypothesis-driven experimentation agent, first image built FROM claude-code rather than platform-base, drives the `nous` Python orchestrator CLI via inherited harness scripts + AGENTS.md/skill, keyless gateway auth, channel-bridge to Slack/Telegram, resume-on-wake; added to harness table + module map); e2e-testing.md (new spec 08-session-delete #1688/#1084, depends on `agent`, self-cleaning). Reviewed no-op (sub-page granularity, files: drift only — left for lint re-pin): ui file-preview-fullscreen dialog #1746, ui sidebar session-deletion client fix #1688, agent-runtime event-loop-block mem/CPU logging #1689, pi-agent preserve user-configured default provider/model #1744, experimental-template sort-last #1741, platform-base build changes (drop gh source build, install-pnpm→JS, mise perms) #1748/#1743/#1741. Watermark bumped to d34c21a. Coverage gap reaffirmed for lint: no dedicated deployment/build-and-CI page — Helm chart + cd.yml (incl. the new build-nous/merge-nous jobs) still have only module-map-level coverage.
## [2026-06-24] lint | at @d34c21a — re-pinned 12 pages drifted by the 380cb06..d34c21a delta whose files: moved but no load-bearing claim changed (verified): agent-runtime + api-server (skills module gained source-roots.ts / public-archive-scanner now shares SKILL_SOURCE_ROOTS — the detail lives in skill-resolution.md, page altitude unchanged; plus #1689 event-loop logging and Dockerfile build changes below altitude), ui (#1746 file-preview-fullscreen dialog, #1688 sidebar-deletion client fix, #1741 template sort — all sub-page granularity), db (deploy/helm/platform/values.yaml moved only to add the nous agent template; postgres/role claims untouched), and connections-and-contributions/hitl-approvals/agent/fork/schedule/session/template/cli (all cite docs/ubiquitous-language.md, whose sole change was the "Skill Source" glossary line — unrelated to each page's claims). Resolved contradiction (handled in this run's ingest): skill-resolution.md no longer states `.claude/skills/` is undiscoverable — #1745 added it. Left untouched (no file moved): agent-client-protocol (acp/ untouched), agent-lifecycle, platform-topology, zero-trust-credential-gateway, envoy-gateway, keycloak, persistence-substrates. 0 orphans, 0 broken links. Coverage gap reaffirmed: no dedicated deployment/build-and-CI page — the Helm chart and cd.yml (now including build-nous/merge-nous and the per-component image/build changes #1748/#1743) still have only module-map-level coverage in dam.md.
## [2026-06-25] ingest | dam-agents/dam @16404fb — delta from d34c21a (1 commit: bump v0.2.6 #1811). Pure release version bump: deploy/helm/platform/Chart.yaml (version/appVersion 0.2.5→0.2.6) and packages/cli/package.json (version 0.2.5→0.2.6). No load-bearing claim affected — no page cites a version number, Chart.yaml has only module-map-level coverage in dam.md, and package.json is outside cli.md's files: (packages/cli/src/). No page edits. Watermark bumped to 16404fb. Coverage gap reaffirmed for lint: still no dedicated deployment/build-and-CI page (Helm chart + cd.yml at module-map-level only).
## [2026-06-25] lint | clean sweep at @16404fb — verified staleness across all 24 pages: the only post-d34c21a change is the v0.2.6 release bump (#1811) touching deploy/helm/platform/Chart.yaml and packages/cli/package.json, neither of which intersects any page's files: (db.md covers the postgres templates + values.yaml but not Chart.yaml; cli.md covers packages/cli/src/ not package.json). No page drifted, no re-pins. 0 orphans, 0 broken links, no contradictions. Coverage gap reaffirmed: still no dedicated deployment/build-and-CI page — the Helm chart (incl. Chart.yaml versioning) and cd.yml have only module-map-level coverage in dam.md.

## [2026-06-26] ingest | dam-agents/dam @d507c05 — delta from 16404fb (6 commits). Two load-bearing features. **#1899 (env spec→contributions, @f09db9c):** user-typed/template/connection/secret env no longer ride the Agent CR's `spec.env` — the controller now projects only platform wiring + chart-level defaults into pod env (resources.go + fork_resources.go), and everything config-tied rides the runtime channel as `env` contributions applied at the next idle turn (no pod roll). New Postgres `agent_env` table (migration 0003) stores user-typed + template env; state-builder emits user env first so it wins on name collision (env driver is first-occurrence-wins). Updated: agent-lifecycle.md (Create-phase env layering rewritten), connections-and-contributions.md (new "The `env` contribution — three sources" section), db.md (agent_env table + 0003/0004 migrations), agent.md (`spec.env` retained-but-unread), template.md (template env seeds `agent_env`, not `spec.env`), e2e-testing.md (new spec 09-user-env). **#1898 (skills from a source subdirectory, @32f9a22):** optional repo-relative `path` on a skill source scanned/resolved *exclusively* (bypasses SKILL_SOURCE_ROOTS union + top-level fallback), denormalized onto `agent_skills` (migration 0004) and carried on the `skill-ref` contribution; escape-guarded at every join site. Updated: skill-resolution.md (new "Optional source subdir (`path`)" section), index.md hook. **Flagged for lint:** spec 09-user-env.spec.ts is committed but no Playwright project registers `/09-.*/` (playwright.config.ts untouched by #1899), so it does not run as configured — recorded on e2e-testing.md, not reconciled. Reviewed no-op (below page altitude / no cited claim affected): #1903 schedules edit-to-fresh persistence fix (schedules-service.ts; schedule.md firing semantics unchanged), skill-source listing precedence flip user→template→system ⇒ user→system→template (no wiki page stated the old order), CLI `dam skill source add --path` flag (cli.md lists groups not flags), values.yaml `skillSources[].path?` shape, bobshell 1.0.4→1.0.5 (#1997) + node 24.14.1→24.18.0 CVE patch (#1998) version bumps, ADR-048 number/status chore (@d507c05; usage-tracking already covered). Watermark bumped to d507c05. Coverage gap reaffirmed for lint: still no dedicated deployment/build-and-CI page (Helm chart + cd.yml at module-map-level only).

## [2026-06-26] lint | at @d507c05 — verified all 24 pages against the 16404fb..d507c05 delta (the same ingest covered). Re-pinned 11 pages drifted by moved files with no load-bearing claim change (verified): agent-client-protocol, fork, schedule, session (all cite docs/architecture/agent-lifecycle.md, whose sole change was the pod-env paragraph rewritten in this run's ingest — ACP relay, fork-as-Job, schedule firing, and session-model claims untouched), agent-runtime (skills-module path plumbing + #1689/Dockerfile below altitude; module map unchanged), agents (bob bobshell 1.0.4→1.0.5 #1997 + platform-base node 24.14.1→24.18.0 #1998 — no version cited on the page), api-server (agents module gained agent-env-repository/backfill-user-env, skills infra gained path support — no new top-level module; "sole writer of spec" + module map unchanged), cli (skill `source add --path` flag — page lists command groups, not flags), controller (pkg/reconciler/ resources.go+fork_resources.go now project only chart-level default env — page writes-only-status/module-map claims unchanged; the env detail lives in agent-lifecycle.md), ui (skills-panel.tsx #1898 — module-map granularity), dam (packages/ moved; no new package, module map unchanged). Left untouched (no file in files: moved): hitl-approvals, persistence-substrates, platform-topology, zero-trust-credential-gateway, envoy-gateway, keycloak. 0 orphans, 0 broken links. Contradiction surfaced (not reconciled, per policy): e2e spec 09-user-env.spec.ts is committed but no Playwright project registers `/09-.*/` (playwright.config.ts untouched by #1899), so it does not run as configured — flagged on e2e-testing.md. Coverage gap reaffirmed: still no dedicated deployment/build-and-CI page — Helm chart (incl. the new `skills.skillSources[].path?` shape) and cd.yml remain at module-map-level coverage in dam.md only.
## [2026-06-27] ingest | dam-agents/dam @e54e8f1 — delta from d507c05 (3 commits). One load-bearing feature. **#2025 (OpenEvolve evolutionary-optimization workload, @a226090):** new `packages/agents/openevolve/` harness — the *second* image built `FROM` the claude-code base (after nous), inheriting the `claude` CLI + model gateway + CA trust and `uv pip`-installing `openevolve==0.2.27` from PyPI into `/opt/openevolve-venv` (prepended to PATH so `openevolve-run` shadows base tools). Conversational AlphaEvolve agent: takes a target repo + measurable objective in chat, authors program(EVOLVE-BLOCK)/evaluator(combined_score)/config.yaml, runs the evolution, reports the winner. Two model paths through one keyless gateway (Claude driver via inherited gateway; evolution loop via OpenAI-compatible OPENAI_BASE_URL/OPENAI_API_KEY from the attached model-provider connection). git/gh/PR via Envoy host-keyed injection (placeholder token only; even evaluator code reaching an allowed GitHub host gets the credential — control surface is the connection scope). Run state under $OPENEVOLVE_OUTPUT_ROOT (~/openevolve-runs) on $HOME PVC ⇒ resume-on-wake after hibernation. AGENTS.md mounted at /etc/AGENTS.md (claude-code symlinks /etc/claude-code/CLAUDE.md→/etc/AGENTS.md) + bundled `openevolve` skill. CI: build-openevolve after merge-agents (pulls claude-code base by per-commit tag) + merge-openevolve publishes multi-arch to quay.io/dam-agents/openevolve; template `category: preconfigured, experimental: true` in values.yaml. Updated: agents.md (new harness-table row + "OpenEvolve — the second claude-code-derived harness" section), dam.md (module-map agents line now lists openevolve as a second claude-code-layered image). Reviewed no-op (below page altitude / no cited claim affected): #1591 bob chat file attachments (bob-acp-shim.mjs now stages `resource_link` upload blocks into the session cwd as text path pointers since Bob can't consume them in ACP mode — sub-page detail under the agents.md bob row; bob has no dedicated page), #2044 ui excludes provider connections from sandbox wizard step 3 (new sandboxes/lib/provider-connections.ts helper — sub-page granularity; ui.md is module-map level), mise.toml openevolve tasks include, values-local.yaml openevolve template entry. Watermark bumped to e54e8f1. Coverage gap reaffirmed for lint: still no dedicated deployment/build-and-CI page — Helm chart + cd.yml (now with build-openevolve/merge-openevolve) remain at module-map-level coverage in dam.md only.
## [2026-06-27] lint | at @e54e8f1 — verified all 24 pages against the d507c05..e54e8f1 delta (the same ingest covered). Re-pinned 2 pages drifted by moved files with no load-bearing claim change (verified): ui.md (packages/ui/src/modules/sandboxes/ — #2044 excludes provider connections from the sandbox wizard step-3 connections list via a new sandboxes/lib/provider-connections.ts helper; sub-page granularity, ui.md is module-map level and states no provider-filtering claim) and db.md (deploy/helm/platform/values.yaml moved only to add the openevolve agentTemplate entry; the page cites values.yaml for the postgres templates/role config, which is untouched). agents.md + dam.md were already refreshed to e54e8f1 in this run's ingest (OpenEvolve harness). Left untouched (no file in files: moved): all 20 other pages — the delta's remaining changes (.github/workflows/cd.yml build-openevolve/merge-openevolve, mise.toml openevolve include, values-local.yaml, packages/agents/bob/* attachment-staging #1591) intersect no other page's files:. 0 orphans, 0 broken links. Contradiction reaffirmed (not reconciled, per policy): e2e spec 09-user-env.spec.ts is still committed with no Playwright project registering /09-.*/ (playwright.config.ts untouched in this delta) — remains flagged on e2e-testing.md. Coverage gap reaffirmed: still no dedicated deployment/build-and-CI page — the Helm chart and cd.yml (now carrying build-openevolve/merge-openevolve alongside build-nous/merge-nous) remain at module-map-level coverage in dam.md only.
